# HANDOFF.md

> Rewritten at the end of every session. Describes the state of the repo *right now*.
> New session: read `AGENTS.md` first, then `PLAN.md`, then this.

**Last session:** 2026-07-13 — Phase 0 (workspace setup)

## Current state

- Fresh Vite + React + TypeScript scaffold; `npm run build` green.
- Deps installed: three 0.185, @react-three/fiber 9.6, @react-three/drei 10.7,
  React 19.2, Vite 8.1 (see package.json for exact versions).
- git initialized on `main`, initial commit contains scaffold + planning docs.
- **No site code exists yet** — App.tsx etc. are still the untouched Vite demo.
- No GitHub remote (Toby is renaming his account; repo will be `<username>.github.io`).

## Next step

**Phase 1 (PLAN.md): theme & layout shell** — replace the Vite demo with the light-theme
design tokens in `src/styles/global.css` and the empty five-section page skeleton.

## Blocked on Toby (see PLAN.md "Open items" for the full table)

- New GitHub username (needed for Phase 7–8)
- Resume path / display name / public email / link URLs (needed for Phase 2)
- Hero visual concept (Phase 4 — placeholder scene until then)
- Higgsfield account (Phase 6 — placeholders until then)

## Warnings / gotchas

- None yet.
